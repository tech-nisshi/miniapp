<div class="content">
  <% @tweets.each do |tweet|%>
    <%= render partial: "tweet", locals:{ tweet: tweet }%>
  <% end %>
  <div class="paginate">
  <%= paginate(@tweets) %>
  </div>
</div>   
</body>
</html>